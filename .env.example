# Prismaで使用するPostgreSQL接続文字列
# ローカル: docker compose up -d で起動したPostgresを指す(pgvector拡張の有効化はPhase 3で使用)
DATABASE_URL="postgresql://ai_forge:ai_forge@localhost:5432/ai_forge?schema=public"

# NextAuth.js (GitHub OAuth)
# GitHub OAuth Appの作成: https://github.com/settings/applications/new
# Authorization callback URL: http://localhost:3000/api/auth/callback/github
GITHUB_CLIENT_ID="GitHub OAuth AppのClient ID"
GITHUB_CLIENT_SECRET="GitHub OAuth AppのClient Secret"
NEXTAUTH_SECRET="openssl rand -base64 32 等で生成したランダムな文字列"
NEXTAUTH_URL="http://localhost:3000"

# GitHubのaccess_token/refresh_tokenをDBに保存する際の暗号化キー
# (openssl rand -base64 32 等で生成した高エントロピーな文字列を指定)
TOKEN_ENCRYPTION_KEY="openssl rand -base64 32 等で生成したランダムな文字列"

# Anthropic API(プロンプト実行機能)
# APIキーの発行: https://console.anthropic.com (API Keys)
ANTHROPIC_API_KEY="sk-ant-..."

# Voyage AI(Phase 3: ドキュメント埋め込み)
# APIキーの発行: https://dashboard.voyageai.com
VOYAGE_API_KEY="pa-..."

# レビューのGitHub Actionsワーカー(.github/workflows/process-pending-reviews.yml)を
# Webhook受信・手動レビュー要求のたびに即時起動するための宛先(src/lib/trigger-review-worker.ts)。
# ai-forgeをフォーク等で自分の名義にデプロイした場合はそのowner/repoに変更する。
GITHUB_ACTIONS_OWNER="takubo29new"
GITHUB_ACTIONS_REPO="ai-forge"
