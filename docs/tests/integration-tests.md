# テスト仕様書 — 結合テスト一覧

| 項目 | 内容 |
| --- | --- |
| 文書名 | ai-forge テスト仕様書(結合テスト一覧) |
| 対象システム | ai-forge(統合AI開発支援プラットフォーム) |
| 版数 | 1.0.0 |
| 対応バージョン | v1.1.0 |
| 作成日 | 2026-08-27 |

[テスト仕様書](../test-specification.md)「4. 結合テスト一覧」の詳細。テスト種別(単体/結合の実行方法)・テスト観点の定義は[テスト仕様書](../test-specification.md)を参照。対象は`src/**/*.integration.test.ts`(実Postgresへ接続、GitHub/Anthropic/Voyage AIはモック、Vitest、`npm run test:integration`で実行)。全25ファイル・153テストケース、APIルート単位でまとめる。

---

## 1. `GET/POST /api/categories`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | GET: 認証がなければ401を返す | 認証 |
| 2 | GET: 自分のカテゴリのみを作成日時の古い順で返し、所属プロンプト数を含む | 正常系/認可 |
| 3 | POST: 認証がなければ401を返す | 認証 |
| 4 | POST: 名前が空なら400を返す | バリデーション |
| 5 | POST: 作成に成功すると201を返す | 正常系 |
| 6 | POST: 同じユーザー内で同名のカテゴリを作ろうとすると409を返す | 一意制約 |

## 2. `PATCH/DELETE /api/categories/:id`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | PATCH: 認証がなければ401を返す | 認証 |
| 2 | PATCH: 他ユーザーのカテゴリには404を返す | 認可 |
| 3 | PATCH: 名前が空なら400を返す | バリデーション |
| 4 | PATCH: 更新に成功すると新しい内容を返す | 正常系 |
| 5 | PATCH: 既存の別カテゴリと同名に更新しようとすると409を返す | 一意制約 |
| 6 | DELETE: 認証がなければ401を返す | 認証 |
| 7 | DELETE: 他ユーザーのカテゴリには404を返す | 認可 |
| 8 | DELETE: 削除に成功すると204を返し、所属プロンプトはカテゴリ未分類のまま残る(SetNull) | データ整合性 |

## 3. `GET/POST /api/prompts`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | GET: 認証がなければ401を返す | 認証 |
| 2 | GET: 自分のプロンプトのみを更新日時の新しい順で返す | 正常系/認可 |
| 3 | GET: `categoryId`で絞り込める | 正常系 |
| 4 | GET: `q`でタイトルを部分一致(大文字小文字を区別しない)絞り込みできる | 正常系 |
| 5 | POST: 認証がなければ401を返す | 認証 |
| 6 | POST: タイトルが空なら400を返す | バリデーション |
| 7 | POST: 本文が空なら400を返す | バリデーション |
| 8 | POST: 他ユーザーの`categoryId`を指定すると400を返す | 認可 |
| 9 | POST: 作成に成功すると201で`versionNumber=1`のバージョンを含めて返す | 正常系 |

## 4. `GET/PATCH/DELETE /api/prompts/:id`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | PATCH: 認証がなければ401を返す | 認証 |
| 2 | PATCH: 他ユーザーのプロンプトには404を返す | 認可 |
| 3 | PATCH: 新しいバージョンを保存すると埋め込みが生成される | 正常系(RAG連携) |
| 4 | PATCH: 埋め込み生成が失敗してもプロンプト保存自体は200のまま返す(ベストエフォート) | ベストエフォート継続性 |
| 5 | PATCH: contentを変更しない更新(タイトルのみ)では埋め込みを生成しない | 正常系(無駄な呼び出しの回避) |
| 6 | GET: 認証がなければ401を返す | 認証 |
| 7 | GET: 他ユーザーのプロンプトには404を返す | 認可 |
| 8 | GET: 存在しないIDには404を返す | 異常系 |
| 9 | GET: 取得に成功すると最新バージョンを含めて返す | 正常系 |
| 10 | DELETE: 認証がなければ401を返す | 認証 |
| 11 | DELETE: 他ユーザーのプロンプトには404を返す | 認可 |
| 12 | DELETE: 削除に成功すると204を返し、バージョンもカスケード削除される | データ整合性 |

## 5. `GET /api/prompts/:id/versions`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | 認証がなければ401を返す | 認証 |
| 2 | 他ユーザーのプロンプトには404を返す | 認可 |
| 3 | バージョン番号の新しい順ですべてのバージョンを返す | 正常系 |

## 6. `POST /api/prompts/:id/execute`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | 認証がなければ401を返す | 認証 |
| 2 | 他ユーザーのプロンプトには404を返す | 認可 |
| 3 | 存在しない`promptVersionId`には400を返す | バリデーション |
| 4 | 成功時はExecutionをSUCCESSで作成し201を返す(埋め込みも生成される) | 正常系(RAG連携) |
| 5 | 埋め込み生成が失敗しても実行結果自体は201のまま返す(ベストエフォート) | ベストエフォート継続性 |
| 6 | AI呼び出し失敗時はExecutionをFAILEDで作成し200を返す(201にしない) | 外部API異常系 |
| 7 | 実行系レート制限の上限に達すると429を返す | レート制限 |

## 7. `GET /api/prompts/:id/executions`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | 認証がなければ401を返す | 認証 |
| 2 | 他ユーザーのプロンプトには404を返す | 認可 |
| 3 | このプロンプトの実行履歴のみを新しい順で返す(他プロンプトの実行結果は含まない) | 正常系/スコープ絞り込み |

## 8. `POST /api/prompts/:id/improvement-suggestions`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | 認証がなければ401を返す | 認証 |
| 2 | 他ユーザーのプロンプトには404を返す | 認可 |
| 3 | 存在しないプロンプトには404を返す | 異常系 |
| 4 | 過去のレビュー指摘が0件の場合は400を返し、Claudeを呼ばない | バリデーション(無駄な呼び出しの回避) |
| 5 | 成功時は200でsummary・suggestionsを返し、Executionを作成しない(このAPI固有の設計判断) | 正常系 |
| 6 | 改善提案系レート制限の上限に達すると429を返す | レート制限 |
| 7 | Claude呼び出し失敗時は502を返す | 外部API異常系 |

## 9. `POST /api/prompt-versions/backfill-embeddings`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | 認証がなければ401を返す | 認証 |
| 2 | 未処理のバージョンが無ければ`processed: 0`を返す | 正常系(境界値) |
| 3 | 埋め込みが無いバージョンにembeddingを追加する | 正常系 |
| 4 | 既に埋め込み済みのバージョンは対象にしない(二重生成防止) | 冪等性 |

## 10. `GET/POST /api/repositories/:id/reviews`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | 他ユーザーのリポジトリには404を返す | 認可 |
| 2 | `{{diff}}`を含まないプロンプトには400を返す | バリデーション |
| 3 | 成功時はClaudeの指摘どおりにReviewCommentを作成し201を返す(新規指摘には都度埋め込みも生成される) | 正常系(RAG連携) |
| 4 | `triggeredVia: CHAT`を指定するとチャット実行として記録される | 正常系(Phase 4連携) |
| 5 | `triggeredVia`に不正な値を指定してもUIとして扱われる | 異常系(入力の頑健性) |
| 6 | diffが切り詰められた場合はReviewCommentに警告を残す | 異常系(外部データの境界値) |
| 7 | AI呼び出し失敗時はReviewをFAILEDで作成し200を返す(201にしない) | 外部API異常系 |

## 11. `POST /api/repositories/:id/documents/sync`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | 認証がなければ401を返す | 認証 |
| 2 | 存在しない/他ユーザーのリポジトリなら404を返す | 認可 |
| 3 | 他ユーザーのリポジトリなら404を返す | 認可 |
| 4 | GitHub連携情報が無ければ400を返す | バリデーション |
| 5 | 取得したMarkdownファイルを`repositoryId`付きのDocumentとして取り込む | 正常系 |
| 6 | ai-forge自身の同期(`repositoryId: null`)とは独立している | データ整合性(複合ユニーク制約) |
| 7 | リポジトリの接続解除でDocumentも削除される | データ整合性(カスケード) |
| 8 | 埋め込み生成に失敗した場合はDocumentを作らない | 外部API異常系 |

## 12. `POST/DELETE /api/reviews/:id/share`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | POST: 認証がなければ401を返す | 認証 |
| 2 | POST: 他ユーザーのレビューには404を返す | 認可 |
| 3 | POST: FAILEDのレビューは共有できず400を返す | バリデーション |
| 4 | POST: SUCCESSのレビューは共有トークンを発行する | 正常系(セキュリティ) |
| 5 | POST: 既に共有済みなら同じトークンを再度返す(冪等) | 冪等性 |
| 6 | DELETE: 他ユーザーのレビューには404を返す | 認可 |
| 7 | DELETE: 共有を解除すると`shareToken`が`null`になる | 正常系(セキュリティ) |

## 13. `POST /api/review-comments/backfill-embeddings`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | 認証がなければ401を返す | 認証 |
| 2 | 未処理の指摘が無ければ`processed: 0`を返す | 正常系(境界値) |
| 3 | 埋め込みが無い指摘にembeddingを追加する | 正常系 |
| 4 | 既に埋め込み済みの指摘は対象にしない | 冪等性 |

## 14. `GET/POST /api/documents`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | POST: 認証がなければ401を返す | 認証 |
| 2 | POST: タイトルまたは本文が空なら400を返す | バリデーション |
| 3 | POST: 見出し単位でチャンク分割し、各チャンクにembeddingを保存して201を返す | 正常系(RAG連携) |
| 4 | POST: 埋め込み生成に失敗した場合はDocumentを作り直せる状態に戻し502を返す | 外部API異常系 |
| 5 | POST: Voyage AIのレート制限(429)時は具体的な案内メッセージを返す | 外部API異常系(専用エラーハンドリング) |
| 6 | GET: 認証がなければ401を返す | 認証 |
| 7 | GET: 自分のドキュメント一覧を新しい順に返す | 正常系/認可 |

## 15. `DELETE /api/documents/:id`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | 他ユーザーのドキュメントには404を返す | 認可 |
| 2 | 削除するとDocumentChunkもカスケード削除され204を返す | データ整合性 |

## 16. `POST /api/documents/sync`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | 認証がなければ401を返す | 認証 |
| 2 | docs配下のMarkdownとルート2ファイル(README・ai-dev-tool-handoff.md)をDocumentとして取り込み、全チャンクに埋め込みが入る | 正常系(RAG連携) |
| 3 | 再実行すると同じ`sourcePath`のDocumentが作り直される(件数は増えない) | 冪等性 |
| 4 | 埋め込み生成に失敗した場合はDocumentを作らない | 外部API異常系 |
| 5 | `repositoryId`無し(ai-forge自身)の同じ`sourcePath`は重複登録できない(部分ユニークインデックス) | データ整合性(DB制約) |

## 17. `POST /api/executions/backfill-embeddings`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | 認証がなければ401を返す | 認証 |
| 2 | 未処理の実行結果が無ければ`processed: 0`を返す | 正常系(境界値) |
| 3 | reviewを伴わないSUCCESS実行にembeddingを追加する | 正常系 |
| 4 | FAILEDな実行は対象にしない | スコープ絞り込み |
| 5 | レビュー由来のSUCCESS実行は対象にしない(ReviewCommentと重複するため除外) | スコープ絞り込み |
| 6 | AI評価由来のSUCCESS実行は対象にしない(暗号化された内容の平文漏えい防止) | スコープ絞り込み/セキュリティ |
| 7 | 既に埋め込み済みの実行結果は対象にしない | 冪等性 |

## 18. `POST /api/chat`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | 認証がなければ401を返す | 認証 |
| 2 | 質問が空なら400を返す | バリデーション |
| 3 | 検索対象が無ければ回答生成をスキップして案内メッセージを返す(Claudeを呼ばない) | 正常系(境界値) |
| 4 | ドキュメント・レビュー両方から検索し、出典付きで回答を返す(距離が近い順に並ぶ) | 正常系(RAG検索) |
| 5 | プロンプトバージョン・実行結果からも検索し、出典付きで回答を返す(Phase 4拡張分) | 正常系(RAG検索) |
| 6 | 他ユーザーの`repositoryId`を指定すると400を返す | 認可 |
| 7 | `repositoryId`指定時は該当リポジトリのDocument・ReviewCommentのみに絞り込む | スコープ絞り込み |
| 8 | チャットからの直接アクション実行: 意図解析でツールが呼ばれたらアクション提案を返し、RAG検索は行わない | 正常系(Phase 4項目4) |
| 9 | チャットからの直接アクション実行: 意図解析でツールが呼ばれなければ通常のRAG回答にフォールバックする | 正常系(フォールバック) |
| 10 | チャットからの直接アクション実行: 実在しないリポジトリ・プロンプトを指すツール呼び出しは無視してフォールバックする | 異常系(外部入力の頑健性) |

## 19. `GET /api/search`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | 認証がなければ401を返す | 認証 |
| 2 | `q`が空ならすべてのグループを空配列で返す | 正常系(境界値) |
| 3 | プロンプト・カテゴリ・リポジトリを大文字小文字を区別せず部分一致で検索する | 正常系 |
| 4 | 他ユーザーのデータは結果に含まれない | 認可 |
| 5 | ドキュメント・評価・レビューも検索対象になる | 正常系(網羅性) |

## 20. `GET/POST /api/evaluations`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | POST: 認証がなければ401を返す | 認証 |
| 2 | POST: 他ユーザーのプロンプトには400を返す | 認可 |
| 3 | POST: 対応していない画像形式には400を返す | バリデーション |
| 4 | POST: 画像サイズの上限(base64換算)を超えると400を返す | 境界値 |
| 5 | POST: 成功時はPENDINGで202を返し、バックグラウンドでEvaluationFindingを作成する(結果は暗号化保存、Executionには平文を残さない、通知も作成される) | 正常系(バックグラウンド処理/セキュリティ) |
| 6 | POST: AI呼び出し失敗時はバックグラウンドでEvaluationをFAILEDにする(失敗通知も作成) | 外部API異常系 |
| 7 | POST: `inputType: TEXT`を指定すると`{{変数名}}`を展開してテキストとしてClaudeに渡す | 正常系(入力形式分岐) |
| 8 | POST: `inputType: TEXT`では画像を指定しなくても400にならない | 正常系(境界値) |
| 9 | POST: `inputType: PDF`でPDFファイルを指定しないと400を返す | バリデーション |
| 10 | POST: PDFサイズの上限(base64換算)を超えると400を返す | 境界値 |
| 11 | POST: `inputType: PDF`を指定するとdocumentブロックとしてClaudeに渡す | 正常系(入力形式分岐) |
| 12 | POST: 評価系レート制限の上限に達すると429を返す | レート制限 |
| 13 | GET: 認証がなければ401を返す | 認証 |
| 14 | GET: 自分のEvaluationのみを新しい順で返す | 正常系/認可 |

## 21. `GET/DELETE /api/evaluations/:id`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | GET: 認証がなければ401を返す | 認証 |
| 2 | GET: 他ユーザーの評価には404を返す | 認可 |
| 3 | GET: 自分の評価はfindings付きで返す | 正常系 |
| 4 | DELETE: 他ユーザーの評価には404を返す(削除もされない) | 認可 |
| 5 | DELETE: 自分の評価を削除できる(findingsもカスケード削除される) | データ整合性 |

## 22. `POST/DELETE /api/evaluations/:id/share`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | POST: 認証がなければ401を返す | 認証 |
| 2 | POST: 他ユーザーの評価には404を返す | 認可 |
| 3 | POST: PENDINGの評価は共有できず400を返す | バリデーション |
| 4 | POST: SUCCESSの評価は共有トークンを発行する | 正常系(セキュリティ) |
| 5 | POST: 既に共有済みなら同じトークンを再度返す(冪等) | 冪等性 |
| 6 | DELETE: 共有を解除すると`shareToken`が`null`になる | 正常系(セキュリティ) |

## 23. `GET /api/notifications`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | 認証がなければ401を返す | 認証 |
| 2 | 自分の通知を新しい順に返し、未読件数も返す(他ユーザーの通知は含まない) | 正常系/認可 |

## 24. `PATCH /api/notifications/:id`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | 認証がなければ401を返す | 認証 |
| 2 | 他ユーザーの通知には404を返す | 認可 |
| 3 | 自分の通知を既読にできる | 正常系 |

## 25. `POST /api/notifications/read-all`

| No. | テストケース | 観点 |
| --- | --- | --- |
| 1 | 認証がなければ401を返す | 認証 |
| 2 | 自分の未読通知をすべて既読にする(他ユーザーには影響しない) | 正常系/スコープ絞り込み |

---

[← テスト仕様書に戻る](../test-specification.md) / [単体テスト一覧](./unit-tests.md)
