{
  "buildCommand": "prisma migrate deploy && next build",
  "ignoreCommand": "if [ \"$VERCEL_ENV\" == \"production\" ]; then exit 1; else exit 0; fi",
  "regions": ["hnd1"],
  "functions": {
    "app/api/queues/review-jobs/route.ts": {
      "experimentalTriggers": [{ "type": "queue/v2beta", "topic": "review-jobs" }]
    }
  }
}
